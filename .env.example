# =============================
# Backend Runtime Configuration
# =============================

# App
APP_PORT_HOST=10000
APP_ADMIN_PASSWORD=change-me-strong-password

# Postgres (docker-compose local)
DB_NAME=esport_tournament
DB_USER=esport
DB_PASSWORD=esport123
DB_PORT_HOST=5432

# Redis (docker-compose local)
REDIS_PORT_HOST=6379

# CORS
ALLOWED_ORIGIN_PATTERNS=http://localhost:3000,http://localhost:5173,http://localhost:8081,http://127.0.0.1:8081,http://10.0.2.2:8081

# Firebase (choose one preferred option)
# 1) Base64 encoded full service-account JSON (recommended for cloud)
FIREBASE_SERVICE_ACCOUNT_BASE64=
# 2) Raw service-account JSON string
FIREBASE_CREDENTIALS=
# 3) Classpath file path (example: firebase-service-account.json)
FIREBASE_SERVICE_ACCOUNT_FILE=

